# A15音频驱动2d虚拟人形象
##1.1 赛题要求

### 1.1.1 必须要达到的基本要求

==1.1 口型的准确度与文本的==

(tip:嘴巴的张开幅度，弯曲程度等等能对得更准)

1.2 保证中文的效果

1.3 单模型训练多种虚拟人形象

1.4 可以没有表情

1.5 只需要提供算法的模型权重以及exe的可执行文件

(tip:把一段python权重和代码剥离他的虚拟环境，打包成一个可执行的exe文件，尽量可以双击后就运行exe文件可能比较大，大小不做限制)

1.6 要求完全脱离互联网，将所有的环境，库函数，打包好

1.7可部署web，也可以只有exe

1.8 文档格式的规范，要有目录，文档更看重算法的说明和讲解



### 1.2.1 额外的加分要求

2.1 上半身的身体更多或整个身体出现在视频里

2.2 手部可以流畅的动作

2.3 同时训练多语言的数据集

 ```markdown
可以是提供 中文一个模型权重，英文一个模型权重。
并不要求保存成一个权重文件
 ```
2.4 做了exe文件的同时，如果也部署了一个==静态页面==

2.5 通过工程化达到模型更加轻量和速度更快的效果

```markdown
精度量化，c++版本的迭代 用openwindow os 这种框架 对模型加速
```

2.6 效果>模型速度，模型大小



## 2.1 相关文件规格

### 2.1.1 视频规格

1. 1024*1024的MP4格式 30fps

   ```markdown
   代码中需要有一个判断 输出的视频跟输入的视频是一个帧率
   ```

   

   

## 3.1 方案选择

### 3.1.1 方案一

运用wav2lip模型，实现对语音驱动嘴型的基本实现



